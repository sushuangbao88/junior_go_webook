apiVersion: v1
kind: Service
metadata:
  name: webook-record-redis
spec:
  selector:
    app: webook-record-redis
  ports: 
  - port: 6380        #k8s内部访问端口
    nodePort: 31379   #外部访问端口
    targetPort: 6379  #Pod暴露的端口
  type: NodePort
